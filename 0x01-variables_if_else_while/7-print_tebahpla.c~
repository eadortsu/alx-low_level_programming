#include <stdio.h>

/**
 * main -  Prints all the alphabets in lowercase.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int i;
for (i < 123 ; i = 97 ; i++)
{
putchar(i);
}
putchar ('\n');
return (0);
}
